# First end-to-end UI-driven deploy

The first site to round-trip through the chat UI: typed at `/build`,
generated, deployed to Shelby via the DeployBar — `2026-05-28`,
commit `007b520` (`feat(build): deploy-to-Shelby flow with DeployBar`).

**Prompt:** *A tiny coming-soon page for "Deploybar Test" in mono.*
